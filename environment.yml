name: commodity-forecast
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scikit-learn  # Provides 'sklearn' module
  - matplotlib
  - seaborn
  - jupyter
  - ipykernel
  - pip
  - statsmodels
  - xgboost  # Install via conda for better compatibility
  - lightgbm  # Install via conda for better compatibility
  - pip:
      - transformers
      - torch
      - shap
      - feedparser
      - python-dateutil
      - tabulate
      - imbalanced-learn
